{
  "name": "riskcheck-server",
  "version": "1.0.0",
  "description": "RiskCheck backend: Telegram bot + API + on-chain deposit polling + Stars payments",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "telegraf": "^4.16.3"
  }
}
