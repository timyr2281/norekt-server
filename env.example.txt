# ── Telegram ──
BOT_TOKEN=123456:ABC...              # from @BotFather
WEBAPP_URL=https://your-miniapp-host # where the riskcheck-miniapp.html is hosted (https)
# PUBLIC_URL is auto-set by Railway as RAILWAY_PUBLIC_DOMAIN; set manually only if needed
# PUBLIC_URL=https://your-app.up.railway.app

# ── Database (Railway PostgreSQL plugin sets this automatically) ──
# DATABASE_URL=postgres://user:pass@host:5432/db

# ── Your receiving wallets (DOUBLE-CHECK) ──
EVM_ADDRESS=0xd6207b9eeaa559699dcf62272ed416881bba8a27
TRON_ADDRESS=TZELSNUrT1vkQJcWYW8vKj8r53HrQ4cFaZ

# ── Block explorer API keys (free) ──
BSCSCAN_KEY=
ARBISCAN_KEY=
TRONGRID_KEY=          # optional, raises rate limit

# ── Tuning ──
POLL_MS=30000          # how often to scan chains (ms)
DEPOSIT_TTL_MIN=60     # pending deposit lifetime (minutes)
PORT=3000
